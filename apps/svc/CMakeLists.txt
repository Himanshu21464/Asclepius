# HTTP/gRPC sidecar for non-C++ tools — stub.
#
# This target is excluded by default (ASCLEPIUS_BUILD_SVC=OFF) until the
# transport layer (httplib or grpc) lands. See docs/ARCHITECTURE.md for
# the planned protocol.
message(STATUS "asclepius-svc: not yet implemented (placeholder target)")
